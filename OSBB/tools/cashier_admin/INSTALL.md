# Cashier Admin v0.2

Replace folder:

```text
OSBB\tools\cashier_admin\
```

`parking_bot.py` does not need replacement if v0.1 is already connected.

Check:

```powershell
python -m py_compile .\OSBB\tools\cashier_admin\cashier_admin_ui.py
python -m py_compile .\OSBB\Bots\parking_bot.py
```
